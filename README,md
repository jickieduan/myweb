# 1.0版本已完成
##当前状态：
语言为php，数据库为mysql。
笔记与记录的保存方式无区别，但是用了两个数据库。
about没写。
在前端中，大多数样式为自己手写。
只能有一个用户。
##2.0要实现的目标
后台改用python。
样式全部改用bootstrap
实现添加电影、书籍、动漫记录时能够同时添加图片，并且列表页以图片显示出来。
实现多人平台，不同用户拥有不同界面。
配套开发手机版（包括android、iso）。
